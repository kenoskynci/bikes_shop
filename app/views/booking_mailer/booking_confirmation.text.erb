Hello <%= @booking.user.name %>,

Thank you for booking a rental. Here is a summary for booking #<%= @booking.id %>:

<%- @booking.items.each do |item| %>
* <%= item.quantity %> <%= item.rental.name %> (<%= number_to_currency item.total_price, :unit => "€" %>)
<%- end %>

Total: <%= number_to_currency @booking.total_price, :unit => "€" %>